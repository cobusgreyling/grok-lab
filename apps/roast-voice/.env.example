# xAI API key for real Grok models (chat + later realtime voice).
# Get one at https://console.x.ai
# Stored in browser localStorage for these demos; for public deploys prefer server-side proxy (/api routes).
XAI_API_KEY=xai-your-key-here

# Optional: override model
# NEXT_PUBLIC_GROK_MODEL=grok-4.3
